[package]
name = "chess-ai"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 3

[dependencies]
arrayvec = "0.7"
burn = { version = "0.20.1", features = [
    "std",
    "tui",
    "train",
    "wgpu",
    "fusion",
    "ndarray",
] }
